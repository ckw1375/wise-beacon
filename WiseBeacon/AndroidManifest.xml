<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wisewells.wisebeacon"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="18"
        android:targetSdkVersion="18" />

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity android:name=".activities.MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.GroupActivity" >
        </activity>

        <service android:name="com.estimote.sdk.service.BeaconService" >
        </service>
        <service
            android:name=".services.WiseService"
            android:enabled="true"
            android:exported="true"
            android:process=":service" >
            <intent-filter>
                <action android:name="com.wisewells.wisebeacon.service" />
            </intent-filter>
        </service>

        <activity android:name="com.wisewells.wisebeacon.activities.GroupAddActivity" >
        </activity>
    </application>

</manifest>